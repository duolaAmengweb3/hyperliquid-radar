{
  "name": "@cexagent/alert-api",
  "version": "0.1.0",
  "description": "HTTP API for hyperliquid-radar alert subscriptions (zero-identity)",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests' && exit 0",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@cexagent/core": "workspace:*",
    "better-sqlite3": "^11.8.0",
    "fastify": "^5.2.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "engines": { "node": ">=20" }
}
