{
  "name": "@cexagent/alert-daemon",
  "version": "0.1.0",
  "description": "Polling daemon that matches alert subscriptions against HL state and pushes to TG/Discord/webhook",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests' && exit 0",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@cexagent/core": "workspace:*",
    "better-sqlite3": "^11.8.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "engines": { "node": ">=20" }
}
